apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ .Values.namespace }}-forward-auth
  namespace: {{ .Values.namespace }}
spec:
  forwardAuth:
    address: {{ .Values.middleware.forwardAuth.address }}
    trustForwardHeader: true
    authResponseHeaders:
      - "X-Auth-User"
